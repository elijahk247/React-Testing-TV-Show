import React from 'react';
import { render } from '@testing-library/react';

import Episodes from '../components/Episodes';

test('Renders the Episodes component', () => {
  render(<Episodes episodes={{}} />)
})